{# empty Twig template #}
<h1>Categories</h1>
{%for category in categories%}
    <p>Category: {{category.name}}</p>
    <a href="{{path('app_category_removecategory', {'id': category.id})}}">Delete</a>
{%endfor%}
{{ form_start(form) }}
{{ form_widget(form) }}
<input type="submit" value="submit">
{{ form_end(form) }}

